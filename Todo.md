# TODOs

- [ ] Validate that the `Report URL` corresponds exactly to the `GitHub Classroom URL` (browser URL).
- [ ] Implement commands to download either an individual repository or all repositories within a selected assignment to a user-specified directory.
- [ ] Export the complete commit history for all students associated with a specified assignment in CSV, Excel, and PDF formats.
- [ ] Format the `Last Push` timestamp using the pattern: `dd-mm-yyyy hh:mm UTC · x days ago`.
- [ ] Standardise commit date formatting to `dd-mm-yy`.