# TODOs

- [ ] Validate that the `Report URL` corresponds exactly to the `GitHub Classroom classroom's URL` (browser URL).
- [ ] Implement commands to download/clone either an individual repository or all repositories within a selected assignment to a user-specified directory.
- [ ] Implement a function that export the complete commit history for all students associated with a specified assignment to CSV, Excel, and PDF formats.
- [ ] Format the `Last push` and `Last pommit` date timestamp using the pattern: `dd-mm-yyyy hh:mm UTC · x days ago`.
- [ ] Standardise commit date formatting to `dd-mm-yy`.