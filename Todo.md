# TODOs

- [x] Validate that the `Report URL` corresponds exactly to the `GitHub Classroom classroom's URL` (browser URL).
- [x] Implement commands to download/clone either an individual repository or all repositories within a selected assignment to a user-specified directory.
- [x] Implement a function that exports the complete commit history for all students associated with a specified assignment to CSV, Excel, and PDF formats.
- [x] Format the `Last push` and `Last pommit` date timestamp using the pattern: `dd-mm-yyyy hh:mm UTC · x days ago`.
- [x] Standardise commit date formatting to `dd-mm-yy`.
